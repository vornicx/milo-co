name: Shopify theme
on: [push, pull_request]
permissions:
  contents: read
jobs:
  theme-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run check
      - run: node --check assets/milo.js
      - run: node scripts/preview.mjs
      - run: npm run package
